---
created: 2026-07-10
updated: "created: 2026-07-10"
---
```json
{
	// 报错：const enum 在隔离模式下不允许
	"isolatedModules": true,
	// 它是 `isolatedModules` 的**进化版 + 增强版**, 不需要再单独写
	// |**强制** type-only import/export 语法|不再只是"建议"，而是"必须"|
	"verbatimModuleSyntax": true
}
```