```text

my-nestjs-app/
├── src/
│   ├── common/                # 🔹 全局通用模块 (跨业务共享)
│   │   ├── decorators/        # 自定义装饰器 (如 @CurrentUser)
│   │   ├── filters/           # 异常过滤器 (HttpExceptionFilter)
│   │   ├── guards/            # 守卫 (JwtAuthGuard, RolesGuard)
│   │   ├── interceptors/      # 拦截器 (TransformInterceptor, Logging)
│   │   ├── pipes/             # 管道 (ZodValidationPipe)
│   │   ├── interfaces/        # 全局 TS 接口/类型定义
│   │   └── utils/             # 工具函数
│   │
│   ├── config/                # 🔹 配置管理
│   │   ├── database.config.ts
│   │   ├── app.config.ts
│   │   └── index.ts           # 统一导出配置工厂
│   │
│   ├── modules/               # 🔹 核心业务模块 (按领域划分)
│   │   ├── auth/              # 认证模块示例
│   │   │   ├── dto/           # ⭐ Zod Schemas & DTOs
│   │   │   │   ├── login.schema.ts
│   │   │   │   └── register.schema.ts
│   │   │   ├── strategies/    # Passport 策略
│   │   │   ├── auth.controller.ts
│   │   │   ├── auth.service.ts
│   │   │   ├── auth.module.ts
│   │   │   └── auth.spec.ts   # 单元测试
│   │   │
│   │   ├── user/              # 用户模块示例
│   │   │   ├── entities/      # ORM 实体 (TypeORM/Prisma/Mongoose)
│   │   │   │   └── user.entity.ts
│   │   │   ├── dto/
│   │   │   │   ├── create-user.schema.ts
│   │   │   │   └── update-user.schema.ts
│   │   │   ├── user.controller.ts
│   │   │   ├── user.service.ts
│   │   │   └── user.module.ts
│   │   │
│   │   └── order/             # 订单模块...
│   │       └── ...
│   │
│   ├── app.module.ts          # 根模块
│   ├── app.controller.ts      # 健康检查等根级路由
│   └── main.ts                # 应用入口
│
├── test/                      # E2E 测试
│   ├── e2e/
│   └── jest-e2e.json
├── .env                       # 环境变量
├── .env.example
├── nest-cli.json
├── tsconfig.json
└── package.json
```
