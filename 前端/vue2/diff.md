双端 diff。四个指针 oldStart/oldEnd/newStart/newEnd 向中间收敛，依次尝试头头、尾尾、头尾、尾头比较；
找不到匹配就建 key→index 映射表去旧列表里查。平均 O(n)，但移动操作多。

双端 diff 是"每轮处理一个节点"的贪心——它每次只保证当前这轮匹配的节点位置对，不计算全局哪些节点应该保持不动。
遇到乱序（多个节点交叉移动、大范围重排）就退化成逐个搬移。


